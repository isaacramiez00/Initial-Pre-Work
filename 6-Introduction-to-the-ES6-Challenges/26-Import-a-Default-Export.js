//If we use the export default statement in another file, we can import a value without using curly brackets {}
//Other than that, same syntax

"use strict";
import subtract from "math_functions";
subtract(7,4);
