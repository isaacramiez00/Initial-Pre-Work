//Parameters are placeholders in functions that we define in our function and then pass arguments when we call our function()

// Example
function ourFunctionWithArgs(a, b) {
  console.log(a - b);
}
ourFunctionWithArgs(10, 5); // Outputs 5

// Only change code below this line.
function functionWithArgs(a, b) {
  console.log(a + b);
}

functionWithArgs(1, 2);
