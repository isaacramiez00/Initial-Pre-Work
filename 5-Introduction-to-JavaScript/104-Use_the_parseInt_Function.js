//parseInt("num"); converts a string to an integer
//if 1st character in str can't be converted then it returns NaN

function convertToInteger(str) {
  return parseInt(str);
}

console.log(convertToInteger("56"));
